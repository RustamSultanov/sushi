version: "3.8"

services:
  rabbitmq:
    image: rabbitmq:3-management 
    container_name: rabbitmq
    volumes:
        #- ./.docker/rabbitmq/etc/:/etc/rabbitmq/
        - ./.docker/rabbitmq/data/:/var/lib/rabbitmq/
        - ./.docker/rabbitmq/logs/:/var/log/rabbitmq/
    ports:
        - 5672:5672
        - 15672:15672

#  backend:
#    restart: always
#    container_name: django
#    build: .
#    command: >
#      sh -c "poetry shell; python manage.py runserver 0.0.0.0:8000"
#    ports:
#      - '8000:8000'
#
#  celery:
#    build: .
#    container_name: 'celery'
#    command: >
#      sh -c "poetry shell; celery -A my_proj worker -l info &&
#             celery -A my_proj beat -l info"

    